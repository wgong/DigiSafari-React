const points = [100, 200]

// const x = points[0]
// const y = points[1]

// De-Structuring array values
const [x, y] = points

console.log(x, y);

const person = {
    firstName: 'John',
    lastName: 'Peter',
    age: 25
}

// De-Structuring objects
const {firstName, lastName, age} = person

console.log(firstName, lastName, age);

